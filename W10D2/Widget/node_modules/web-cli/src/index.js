'use strict'

/**
  * Transforms a DOM element to webCLI 
  * 
  * Supported DOM object parameter types:
  * [ input, div contenteditable, textarea ]
  * 
  * Example usage:
  * 
  * var webCLI = require('web-cli');
  * let myInput = new webCLI($('#myInput'));
  */
var webCLI = function(inputDomElement) {
  console.log('ooo ! makeItCLI ' + test);
};

/**
  * Adds meat to webCLI object
  * 
  * Takes a map of commands with their: parameters
  * (optional), descriptions (optional) and
  * callback functions.
  * 
  * E.g. below pager object defines a 'page' command
  * with description 'To page people' and parameters:
  * 'Kamil, Tom, Jerry' and callback function
  * pagePerson
  * 
  * let pager = { 
  *   command: 'page',
  *   description: 'To page people',
  *   parameters: [ 'Kamil', 'Tom', 'Jerry' ],
  *   callback: pagePerson
  * };
  *
  * myInput.addCommands(pager);
  * 
  */
webCLI.prototype.addCommands = function() {
  console.log('ooo ! addCommands');
}

module.exports = webCLI;